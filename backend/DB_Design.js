const Room = {
  roomName,
  isGroup,
  users: [
    { uid1, role },
    { uid2, role },
  ],
  last_msg,
};

const Message = {
  roomId,
  senderId,
  msg,
  unSend,
};

const User = {
  avatar,
  banner,
  name,
  password,
  phone,
  gender,
  dob,
};
